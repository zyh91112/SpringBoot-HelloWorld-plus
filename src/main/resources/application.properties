# 当前激活的配置文件 application-{active}.properties
spring.profiles.active=dev
# 关闭favicon.ico，开启的话请将自定义的favicon.ico 放在resources/static目录下
spring.mvc.favicon.enabled=false
# 日志路径
logging.path=log
# 日志保存天数
logging.file.max-history=2
# 全局的日志级别
#logging.level.root= DEBUG
# org.springframework.web包的日志输出级别
logging.level.org.springframework.web=DEBUG
spring.datasource.url=jdbc:mysql://localhost/spring_demo?useSSL=false
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
############ jpa与hibernate配置 ##################
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
# 美化输出的sql语句，使其带有缩进
spring.jpa.properties.hibernate.format_sql=true

# 美化输出的JSON，使其带有缩进
spring.jackson.serialization.indent-output=true

# HTTP response compression HTTP响应压缩
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain
# 仅压缩2048 byte 以上的response
server.compression.min-response-size=2048



